.playwright-cli/
node_modules/

# Outreach tooling is INTERNAL — never publish scraped business data.
# leads.json contains names/addresses/phones of real businesses (DPDP Act 2023,
# Google Places ToS). targeting-guide.md is internal sales strategy.
outreach/lead-scanner/leads.json
outreach/lead-scanner/leads.csv
outreach/lead-scanner/dashboard.html
outreach/targeting-guide.md
**/.env

# Wrangler build cache and local secret overrides for the enquiry Worker.
.wrangler/
worker/.dev.vars
worker/preview/

# Root tooling deps (test harness)
/node_modules/

# Local Claude Code permissions — held a Google Places API key in a public repo.
# Keep it out of the repo; secrets belong in an untracked .env.
.claude/settings.json

# Superseded 2026 portfolio drafts. Kept on disk, unpublished — they carry the
# old personal-portfolio identity the studio voice replaced.
/v1.html
/v2.html
/uplift.html
/Mayank Portfolio.html

# Ad copy, Reel scripts and Ads Manager settings — same category as
# targeting-guide.md above, and this repo is served at netloom.in.
outreach/instagram-content-kit.md
outreach/Netloom-Instagram-Content-Kit.pdf
