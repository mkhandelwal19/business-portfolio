{
  "name": "netloom-enquiry",
  "version": "1.0.0",
  "private": true,
  "description": "Contact form transport for netloom.in — sends via Zoho SMTP.",
  "comment": "Scripts rather than bare npx: a stale global npx (npm 6 era) in AppData/Roaming/npm shadows Node's own and dies with 'cb.apply is not a function'. npm run puts node_modules/.bin on PATH and sidesteps it.",
  "scripts": {
    "login": "wrangler login",
    "whoami": "wrangler whoami",
    "secret": "wrangler secret put",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
